/**
 * FastReport Cloud
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CloudBaseVM from './CloudBaseVM';

/**
 * The SelectedFilesVM model module.
 * @module models/SelectedFilesVM
 * @version v1
 */
class SelectedFilesVM {
    /**
     * Constructs a new <code>SelectedFilesVM</code>.
     * @alias module:models/SelectedFilesVM
     * @extends module:models/CloudBaseVM
     * @implements module:models/CloudBaseVM
     * @param t {String} 
     */
    constructor(t) { 
        CloudBaseVM.initialize(this, t);
        SelectedFilesVM.initialize(this, t);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, t) { 
        obj['$t'] = t;
    }

    /**
     * Constructs a <code>SelectedFilesVM</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:models/SelectedFilesVM} obj Optional instance to populate.
     * @return {module:models/SelectedFilesVM} The populated <code>SelectedFilesVM</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SelectedFilesVM();
            CloudBaseVM.constructFromObject(data, obj);
            CloudBaseVM.constructFromObject(data, obj);

            if (data.hasOwnProperty('isAllSelected')) {
                obj['isAllSelected'] = ApiClient.convertToType(data['isAllSelected'], 'Boolean');
            }
            if (data.hasOwnProperty('folderId')) {
                obj['folderId'] = ApiClient.convertToType(data['folderId'], 'String');
            }
            if (data.hasOwnProperty('searchPattern')) {
                obj['searchPattern'] = ApiClient.convertToType(data['searchPattern'], 'String');
            }
            if (data.hasOwnProperty('useRegex')) {
                obj['useRegex'] = ApiClient.convertToType(data['useRegex'], 'Boolean');
            }
            if (data.hasOwnProperty('files')) {
                obj['files'] = ApiClient.convertToType(data['files'], ['String']);
            }
            if (data.hasOwnProperty('folders')) {
                obj['folders'] = ApiClient.convertToType(data['folders'], ['String']);
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('isBin')) {
                obj['isBin'] = ApiClient.convertToType(data['isBin'], 'Boolean');
            }
            if (data.hasOwnProperty('$t')) {
                obj['$t'] = ApiClient.convertToType(data['$t'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SelectedFilesVM</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SelectedFilesVM</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SelectedFilesVM.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['folderId'] && !(typeof data['folderId'] === 'string' || data['folderId'] instanceof String)) {
            throw new Error("Expected the field `folderId` to be a primitive type in the JSON string but got " + data['folderId']);
        }
        // ensure the json data is a string
        if (data['searchPattern'] && !(typeof data['searchPattern'] === 'string' || data['searchPattern'] instanceof String)) {
            throw new Error("Expected the field `searchPattern` to be a primitive type in the JSON string but got " + data['searchPattern']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['files'])) {
            throw new Error("Expected the field `files` to be an array in the JSON data but got " + data['files']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['folders'])) {
            throw new Error("Expected the field `folders` to be an array in the JSON data but got " + data['folders']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }
        // ensure the json data is a string
        if (data['$t'] && !(typeof data['$t'] === 'string' || data['$t'] instanceof String)) {
            throw new Error("Expected the field `$t` to be a primitive type in the JSON string but got " + data['$t']);
        }

        return true;
    }


}

SelectedFilesVM.RequiredProperties = ["$t"];

/**
 * @member {Boolean} isAllSelected
 */
SelectedFilesVM.prototype['isAllSelected'] = undefined;

/**
 * @member {String} folderId
 */
SelectedFilesVM.prototype['folderId'] = undefined;

/**
 * @member {String} searchPattern
 */
SelectedFilesVM.prototype['searchPattern'] = undefined;

/**
 * @member {Boolean} useRegex
 */
SelectedFilesVM.prototype['useRegex'] = undefined;

/**
 * @member {Array.<String>} files
 */
SelectedFilesVM.prototype['files'] = undefined;

/**
 * @member {Array.<String>} folders
 */
SelectedFilesVM.prototype['folders'] = undefined;

/**
 * @member {String} path
 */
SelectedFilesVM.prototype['path'] = undefined;

/**
 * @member {Boolean} isBin
 */
SelectedFilesVM.prototype['isBin'] = undefined;

/**
 * @member {String} $t
 */
SelectedFilesVM.prototype['$t'] = undefined;


// Implement CloudBaseVM interface:
/**
 * @member {String} $t
 */
CloudBaseVM.prototype['$t'] = undefined;




export default SelectedFilesVM;

