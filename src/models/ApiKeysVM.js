/**
 * FastReport Cloud
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ApiKeyVM from './ApiKeyVM';
import CloudBaseVM from './CloudBaseVM';

/**
 * The ApiKeysVM model module.
 * @module models/ApiKeysVM
 * @version v1
 */
class ApiKeysVM {
    /**
     * Constructs a new <code>ApiKeysVM</code>.
     * @alias module:models/ApiKeysVM
     * @extends module:models/CloudBaseVM
     * @implements module:models/CloudBaseVM
     * @param t {String} 
     */
    constructor(t) { 
        CloudBaseVM.initialize(this, t);
        ApiKeysVM.initialize(this, t);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, t) { 
        obj['$t'] = t;
    }

    /**
     * Constructs a <code>ApiKeysVM</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:models/ApiKeysVM} obj Optional instance to populate.
     * @return {module:models/ApiKeysVM} The populated <code>ApiKeysVM</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiKeysVM();
            CloudBaseVM.constructFromObject(data, obj);
            CloudBaseVM.constructFromObject(data, obj);

            if (data.hasOwnProperty('apiKeys')) {
                obj['apiKeys'] = ApiClient.convertToType(data['apiKeys'], [ApiKeyVM]);
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('$t')) {
                obj['$t'] = ApiClient.convertToType(data['$t'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiKeysVM</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiKeysVM</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiKeysVM.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['apiKeys']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['apiKeys'])) {
                throw new Error("Expected the field `apiKeys` to be an array in the JSON data but got " + data['apiKeys']);
            }
            // validate the optional field `apiKeys` (array)
            for (const item of data['apiKeys']) {
                ApiKeyVM.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['$t'] && !(typeof data['$t'] === 'string' || data['$t'] instanceof String)) {
            throw new Error("Expected the field `$t` to be a primitive type in the JSON string but got " + data['$t']);
        }

        return true;
    }


}

ApiKeysVM.RequiredProperties = ["$t"];

/**
 * @member {Array.<module:models/ApiKeyVM>} apiKeys
 */
ApiKeysVM.prototype['apiKeys'] = undefined;

/**
 * @member {Number} count
 */
ApiKeysVM.prototype['count'] = undefined;

/**
 * @member {String} $t
 */
ApiKeysVM.prototype['$t'] = undefined;


// Implement CloudBaseVM interface:
/**
 * @member {String} $t
 */
CloudBaseVM.prototype['$t'] = undefined;




export default ApiKeysVM;

