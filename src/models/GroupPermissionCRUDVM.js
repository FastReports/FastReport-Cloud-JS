/**
 * FastReport Cloud
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CloudBaseVM from './CloudBaseVM';
import GroupAdministrate from './GroupAdministrate';
import GroupCreate from './GroupCreate';
import GroupDelete from './GroupDelete';
import GroupExecute from './GroupExecute';
import GroupGet from './GroupGet';
import GroupUpdate from './GroupUpdate';

/**
 * The GroupPermissionCRUDVM model module.
 * @module models/GroupPermissionCRUDVM
 * @version v1
 */
class GroupPermissionCRUDVM {
    /**
     * Constructs a new <code>GroupPermissionCRUDVM</code>.
     * @alias module:models/GroupPermissionCRUDVM
     * @extends module:models/CloudBaseVM
     * @implements module:models/CloudBaseVM
     * @param t {String} 
     */
    constructor(t) { 
        CloudBaseVM.initialize(this, t);
        GroupPermissionCRUDVM.initialize(this, t);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, t) { 
        obj['$t'] = t;
    }

    /**
     * Constructs a <code>GroupPermissionCRUDVM</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:models/GroupPermissionCRUDVM} obj Optional instance to populate.
     * @return {module:models/GroupPermissionCRUDVM} The populated <code>GroupPermissionCRUDVM</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupPermissionCRUDVM();
            CloudBaseVM.constructFromObject(data, obj);
            CloudBaseVM.constructFromObject(data, obj);

            if (data.hasOwnProperty('create')) {
                obj['create'] = GroupCreate.constructFromObject(data['create']);
            }
            if (data.hasOwnProperty('delete')) {
                obj['delete'] = GroupDelete.constructFromObject(data['delete']);
            }
            if (data.hasOwnProperty('execute')) {
                obj['execute'] = GroupExecute.constructFromObject(data['execute']);
            }
            if (data.hasOwnProperty('get')) {
                obj['get'] = GroupGet.constructFromObject(data['get']);
            }
            if (data.hasOwnProperty('update')) {
                obj['update'] = GroupUpdate.constructFromObject(data['update']);
            }
            if (data.hasOwnProperty('administrate')) {
                obj['administrate'] = GroupAdministrate.constructFromObject(data['administrate']);
            }
            if (data.hasOwnProperty('$t')) {
                obj['$t'] = ApiClient.convertToType(data['$t'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupPermissionCRUDVM</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupPermissionCRUDVM</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GroupPermissionCRUDVM.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['$t'] && !(typeof data['$t'] === 'string' || data['$t'] instanceof String)) {
            throw new Error("Expected the field `$t` to be a primitive type in the JSON string but got " + data['$t']);
        }

        return true;
    }


}

GroupPermissionCRUDVM.RequiredProperties = ["$t"];

/**
 * @member {module:models/GroupCreate} create
 */
GroupPermissionCRUDVM.prototype['create'] = undefined;

/**
 * @member {module:models/GroupDelete} delete
 */
GroupPermissionCRUDVM.prototype['delete'] = undefined;

/**
 * @member {module:models/GroupExecute} execute
 */
GroupPermissionCRUDVM.prototype['execute'] = undefined;

/**
 * @member {module:models/GroupGet} get
 */
GroupPermissionCRUDVM.prototype['get'] = undefined;

/**
 * @member {module:models/GroupUpdate} update
 */
GroupPermissionCRUDVM.prototype['update'] = undefined;

/**
 * @member {module:models/GroupAdministrate} administrate
 */
GroupPermissionCRUDVM.prototype['administrate'] = undefined;

/**
 * @member {String} $t
 */
GroupPermissionCRUDVM.prototype['$t'] = undefined;


// Implement CloudBaseVM interface:
/**
 * @member {String} $t
 */
CloudBaseVM.prototype['$t'] = undefined;




export default GroupPermissionCRUDVM;

