/**
 * FastReport Cloud
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExportCreateVM from './ExportCreateVM';

/**
 * The ExportCreateAdminVM model module.
 * @module models/ExportCreateAdminVM
 * @version v1
 */
class ExportCreateAdminVM {
    /**
     * Constructs a new <code>ExportCreateAdminVM</code>.
     * @alias module:models/ExportCreateAdminVM
     * @extends module:models/ExportCreateVM
     * @implements module:models/ExportCreateVM
     * @param t {String} 
     */
    constructor(t) { 
        ExportCreateVM.initialize(this, t);
        ExportCreateAdminVM.initialize(this, t);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, t) { 
        obj['ownerId'] = ownerId;
        obj['parentId'] = parentId;
        obj['$t'] = t;
    }

    /**
     * Constructs a <code>ExportCreateAdminVM</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:models/ExportCreateAdminVM} obj Optional instance to populate.
     * @return {module:models/ExportCreateAdminVM} The populated <code>ExportCreateAdminVM</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExportCreateAdminVM();
            ExportCreateVM.constructFromObject(data, obj);
            ExportCreateVM.constructFromObject(data, obj);

            if (data.hasOwnProperty('ownerId')) {
                obj['ownerId'] = ApiClient.convertToType(data['ownerId'], 'String');
            }
            if (data.hasOwnProperty('parentId')) {
                obj['parentId'] = ApiClient.convertToType(data['parentId'], 'String');
            }
            if (data.hasOwnProperty('$t')) {
                obj['$t'] = ApiClient.convertToType(data['$t'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExportCreateAdminVM</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExportCreateAdminVM</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExportCreateAdminVM.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ownerId'] && !(typeof data['ownerId'] === 'string' || data['ownerId'] instanceof String)) {
            throw new Error("Expected the field `ownerId` to be a primitive type in the JSON string but got " + data['ownerId']);
        }
        // ensure the json data is a string
        if (data['parentId'] && !(typeof data['parentId'] === 'string' || data['parentId'] instanceof String)) {
            throw new Error("Expected the field `parentId` to be a primitive type in the JSON string but got " + data['parentId']);
        }
        // ensure the json data is a string
        if (data['$t'] && !(typeof data['$t'] === 'string' || data['$t'] instanceof String)) {
            throw new Error("Expected the field `$t` to be a primitive type in the JSON string but got " + data['$t']);
        }

        return true;
    }


}

ExportCreateAdminVM.RequiredProperties = ["ownerId", "parentId", "$t"];

/**
 * @member {String} ownerId
 */
ExportCreateAdminVM.prototype['ownerId'] = undefined;

/**
 * @member {String} parentId
 */
ExportCreateAdminVM.prototype['parentId'] = undefined;

/**
 * @member {String} $t
 */
ExportCreateAdminVM.prototype['$t'] = undefined;


// Implement ExportCreateVM interface:
/**
 * @member {String} name
 */
ExportCreateVM.prototype['name'] = undefined;
/**
 * @member {Array.<String>} tags
 */
ExportCreateVM.prototype['tags'] = undefined;
/**
 * @member {Blob} icon
 */
ExportCreateVM.prototype['icon'] = undefined;
/**
 * @member {Blob} content
 */
ExportCreateVM.prototype['content'] = undefined;
/**
 * @member {String} $t
 */
ExportCreateVM.prototype['$t'] = undefined;




export default ExportCreateAdminVM;

