/**
 * FastReport Cloud
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CloudBaseVM from './CloudBaseVM';
import SubscriptionPlanVM from './SubscriptionPlanVM';

/**
 * The RestOfSpaceVM model module.
 * @module models/RestOfSpaceVM
 * @version v1
 */
class RestOfSpaceVM {
    /**
     * Constructs a new <code>RestOfSpaceVM</code>.
     * @alias module:models/RestOfSpaceVM
     * @extends module:models/CloudBaseVM
     * @implements module:models/CloudBaseVM
     * @param t {String} 
     */
    constructor(t) { 
        CloudBaseVM.initialize(this, t);
        RestOfSpaceVM.initialize(this, t);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, t) { 
        obj['$t'] = t;
    }

    /**
     * Constructs a <code>RestOfSpaceVM</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:models/RestOfSpaceVM} obj Optional instance to populate.
     * @return {module:models/RestOfSpaceVM} The populated <code>RestOfSpaceVM</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RestOfSpaceVM();
            CloudBaseVM.constructFromObject(data, obj);
            CloudBaseVM.constructFromObject(data, obj);

            if (data.hasOwnProperty('restOfSpace')) {
                obj['restOfSpace'] = ApiClient.convertToType(data['restOfSpace'], 'Number');
            }
            if (data.hasOwnProperty('subscriptionPlan')) {
                obj['subscriptionPlan'] = SubscriptionPlanVM.constructFromObject(data['subscriptionPlan']);
            }
            if (data.hasOwnProperty('$t')) {
                obj['$t'] = ApiClient.convertToType(data['$t'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RestOfSpaceVM</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RestOfSpaceVM</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RestOfSpaceVM.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `subscriptionPlan`
        if (data['subscriptionPlan']) { // data not null
          SubscriptionPlanVM.validateJSON(data['subscriptionPlan']);
        }
        // ensure the json data is a string
        if (data['$t'] && !(typeof data['$t'] === 'string' || data['$t'] instanceof String)) {
            throw new Error("Expected the field `$t` to be a primitive type in the JSON string but got " + data['$t']);
        }

        return true;
    }


}

RestOfSpaceVM.RequiredProperties = ["$t"];

/**
 * @member {Number} restOfSpace
 */
RestOfSpaceVM.prototype['restOfSpace'] = undefined;

/**
 * @member {module:models/SubscriptionPlanVM} subscriptionPlan
 */
RestOfSpaceVM.prototype['subscriptionPlan'] = undefined;

/**
 * @member {String} $t
 */
RestOfSpaceVM.prototype['$t'] = undefined;


// Implement CloudBaseVM interface:
/**
 * @member {String} $t
 */
CloudBaseVM.prototype['$t'] = undefined;




export default RestOfSpaceVM;

